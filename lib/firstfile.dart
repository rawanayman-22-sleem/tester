import 'package:flutter/material.dart';

class firstfile extends StatelessWidget {
  const firstfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
